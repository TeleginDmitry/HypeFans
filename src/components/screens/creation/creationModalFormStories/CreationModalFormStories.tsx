import React from 'react'
import styles from './CreationModalFormStories.module.scss'
import { createPortal } from 'react-dom'

const CreationModalFormStories = () => {
	return createPortal(

  <div className={styles.wrapper}>
    
  </div>

  , document.body)
}

export default CreationModalFormStories
