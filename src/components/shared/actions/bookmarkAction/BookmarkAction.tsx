import { IconProps, Bookmark } from 'icons-hypefans-lib'

import './BookmarkAction.module.scss'

const BookmarkAction = (props: IconProps) => {
  return <Bookmark {...props}></Bookmark>
}

export default BookmarkAction
